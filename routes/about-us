const express = require('express');
const router = express.Router();

const getAboutUs = require('../controllers/about-us-controller');

router.get('/about-us', getAboutUs);

module.exports = router;
